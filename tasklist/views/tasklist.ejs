<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title> <%= title %></title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://code.jquery.com/jquery-2.2.4.min.js"></script>
</head>
<body>
<h2><%= title %></h2>
<form name="userForm" action="/input.js" >
    <input type="hidden" name="id" value="0" />
    <div class="form-group">
        <label for="name"><%= name %></label>
        <input class="form-control" name="taskName" />
    </div>
    <div class="form-group">
        <label for="date"><%= date %></label>
        <input class="form-control" type="datetime-local" name="date" />
    </div>
    <div >
        <label for="attachment"><%= attachment %></label>
        <input class="form-control" type="file" name="attachment" />
    </div>
    <div>
        <label for="isCompleted"><%= isCompleted %>:</label>
        <input class="form-control" type="checkbox" name="attachment" />
    </div>
    <div class="panel-body">
        <button type="submit" class="btn btn-sm btn-primary"><%= save %></button>
        <input type="reset" class="btn btn-sm btn-primary" value="<%= reset %>">
    </div>
</form>
<table class="table table-condensed table-striped table-bordered">
    <thead><tr><th><%= id %></th><th><%= name %></th><th><%= date %></th><th><%= attachment %></th><th></th></tr></thead>
    <tbody>
    <% for (let i = 0; i < content.length; i++) {%>
        <tr data-rowid='<%= content[i].id.toString() %>'>
        <td><%= content[i].id.toString() %></td>
        <td><%= content[i].name %></td>
        <td><%= content[i].date %></td>
        <td><%= content[i].attachment %></td>
        <td>
            <input type="button" class='editLink' data-id='<%= content[i].id.toString() %>' value="<%= edit %>">
            <input type="button" class='removeLink' data-id='<%= content[i].id.toString() %>' value="<%= remove %>">
        </td>
        </tr>
    <%} %>
    </tbody>
</table>

</body>
</html>